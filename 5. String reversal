Aim: Reverse a string using a stack.

Algorithm:
start
the string need to be reversed.
then
Push all chars to stack.
Pop them to form reversed string.
display the reversed string
end

Code:
#include <stdio.h>
#include <stdlib.h>

int TOP = - 1,Max;
char *Stack,item;
void push{
if (TOP == Max-1){
printf("Stack overflow")
}
else:
Top = Top+1;
Stack[Top]= item;
}
}
char pop(){
if (Top == -1){
printf("stack is underflow");
return 0;
}
else{
item = Stack[TOP];
TOP = TOP-1;
return item;
}
void reversal(){
char string[MAX];
printf("Please enter the size of string");
scanf("%s",string)
for (int c= 0 ,c<Max,c++){
push(string(c))}
printf("Reversed strin g: ");
for(intc=0;c<Max;c++){
printf("%c",pop());
}
printf("\n");
}

int main(){
printf("Enter the string");
scanf("%d",&Max);

stack= char*realloc(Stack , Max);

string Reversal();
return 0
}

output:
please enter the size of the string :7
enter the string:Nandini
reversed string:inidnaN

result:
Shows stack use; O(n) time, O(n) extra space.
