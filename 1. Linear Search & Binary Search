Aim:
To search an element in an array using linear and binary search techniques.

Algorithm:
Linear Search:
Start
Compare the target element with each array element
If match found, return position
If end reached, return not found
Stop
Binary Search:
Start
Set low = 0, high = n-1
Repeat until low <= high:
mid = (low + high)/2
If arr[mid] == key → found
If key < arr[mid] → high = mid - 1
Else → low = mid + 1
Stop

code:

#include <stdio.h>


void linearSearch(int arr[], int n, int key) {
for(int i = 0; i < n; i++) {
if(arr[i] == key) {
printf("Linear Search: Found at position %d\n", i+1);
return;
}
}
printf("Linear Search: Not Found\n");
}


void binarySearch(int arr[], int n, int key) {
int low = 0, high = n - 1;


while(low <= high) {
int mid = (low + high) / 2;
if(arr[mid] == key) {
printf("Binary Search: Found at position %d\n", mid+1);
return;
}
else if(key < arr[mid])
high = mid - 1;
else
low = mid + 1;
}


printf("Binary Search: Not Found\n");
}


int main() {
int arr[] = {10, 20, 30, 40, 50};
int key = 30;
int n = 5;

Output:
Linear Search: Found at position 3
Binary Search: Found at position 3

linearSearch(arr, n, key);
binarySearch(arr, n, key);
return 0;
}
